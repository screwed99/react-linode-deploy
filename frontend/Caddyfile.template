:80 {
    # The :80 explicitly tells Caddy "HTTP ONLY".
    # It disables all Auto-HTTPS logic.
    root * /srv
    encode gzip
    file_server

    # SPA Fallback: If file not found, serve index.html
    try_files {path} /index.html
}